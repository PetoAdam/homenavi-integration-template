apiVersion: v1
kind: Service
metadata:
  name: {{ include "integration-template.fullname" . }}
  labels:
    {{- include "integration-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "integration-template.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
